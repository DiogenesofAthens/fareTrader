# Exclude local-only files from the Vercel bundle.
# app.py is kept — Vercel uses it as the FastAPI entrypoint,
# but it guards SQLite imports behind the VERCEL env var.
*.db
*.db-shm
*.db-wal
__pycache__/
